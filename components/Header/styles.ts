import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    padding: 16,
    width: '100%',
  },
  text: {
    fontSize: 20,
    fontWeight: 'bold',
  },
});

export default styles;
