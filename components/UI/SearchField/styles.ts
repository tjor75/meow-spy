import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  searchField: {
    flex: 1,
    borderRadius: 50,
    paddingHorizontal: 8,
    paddingVertical: 5,
    fontSize: 16,
  },
});

export default styles;
