import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    position: "relative",
  },
  containerLandscape: {
    flex: 0.8,
  },
  containerPortrait: {
    height: 250,
  },
});

export default styles;
